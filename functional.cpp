/*
* Explore functional part of c++
*/

#include<functional>
#include<iostream>

using namespace std;
//std::function is a general purpose polymorphic wrapper that can store, copy, invoke
//any "CALLABLE" target: functions, lambda expressions, bind expressions, function objects
//as well as pointers to memeber functions and data members(that are callable)

//callable object is called target
//if target is empty and std::function is called std::bad_function_call exception is thrown
int main()
{


}
